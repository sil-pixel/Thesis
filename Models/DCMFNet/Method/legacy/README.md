# Legacy research workflows

These scripts reproduce the original multi-seed thesis analysis and remain for
compatibility with files under `Models/DCMFNet/Analysis`. They are not used by
the supported training/export pipeline or inference API.

New development belongs in `dcmfnet/`; do not add deployment dependencies here.
